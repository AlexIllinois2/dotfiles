[global]
# 阿里云 PyPI 镜像（清华源当前 403，改用阿里云）
index-url = https://mirrors.aliyun.com/pypi/simple
timeout = 6000
# 开启缓存、并行下载加速
cache-dir = ~/.cache/pip
[install]
trusted-host = mirrors.aliyun.com
# 全局开启依赖并行安装
parallel-builds = 10
