## 00. Language (最高优先级)
- 强制中文回复（代码、标识符、报错原文除外）

## 0. PackageManager
- Python→`uv`；Node→`bun`→`pnpm`（禁`npm`），lock 随工具
- Node `package.json` 须声明 `packageManager`

## 1. Build
- 禁打包/构建/发布（dev/build/release）；仅允许 lint/check/type-check
